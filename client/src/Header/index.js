import { Wrapper, Logo, Buttons } from './Header.styles';

const Header = () => {
    
    return (
        <Wrapper>
        <Logo>
            AVW
        </Logo>
        <Buttons>
        Portfolio
        Contact
        </Buttons>
        </Wrapper>
    );
    }

export default Header;